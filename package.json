{
  "name": "mocha-demo",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha",
    "test:coverage": "nyc --reporter=html --reporter=lcov --reporter=text mocha"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "chai": "^4.0.0",
    "nyc": "^15.0.0"
  }
}
